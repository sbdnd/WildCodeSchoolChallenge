{{ form_start(form) }}
    <div class="row justify-content-center">
        <div class="col-12 text-center">{{ form_row(form.name, {'label': 'Nom'})}}</div>
    <button class="btn btn-primary mt-2">{{ button_label|default('Envoyer') }}</button>
    </div>
    {{ form_widget(form) }}
    {# <a href="{{ path('argonaute_index') }}">retour à la liste</a> #}
{{ form_end(form) }}
