{% extends 'base.html.twig' %}

{% block title %}Argonaute{% endblock %}

{% block body %}
    <h1>Argonaute</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ argonaute.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ argonaute.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('argonaute_index') }}">back to list</a>

    <a href="{{ path('argonaute_edit', {'id': argonaute.id}) }}">edit</a>

    {{ include('argonaute/_delete_form.html.twig') }}
{% endblock %}
